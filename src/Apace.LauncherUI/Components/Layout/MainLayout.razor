@inherits LayoutComponentBase
@using Apace.LauncherUI.Utils
@inject UpdateCheckService UpdateCheck

<div class="page">
    <div class="sidebar">
        <NavMenu />
    </div>

    <main>
        <div class="topbar">
            <button class="theme-btn" onclick="toggleDarkMode()" title="Toggle dark mode">
                <span class="dmb-moon">☀ LIGHT</span>
                <span class="dmb-sun">🌙 DARK</span>
            </button>
            @if (UpdateCheck.State == UpdateCheckState.UpdateAvailable)
            {
                <a href="/About" class="update-chip" title="A newer Apace version is available — see the About page for update instructions">▲ Update available</a>
            }
            <a href="/About" class="topbar-link">About</a>
        </div>

        <article class="content">
            @Body
        </article>
    </main>
</div>

<div id="blazor-error-ui" data-nosnippet>
    An unhandled error has occurred.
    <a href="." class="reload">Reload</a>
    <span class="dismiss">🗙</span>
</div>
